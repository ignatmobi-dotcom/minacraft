# Minacraft — План проекта

## Концепция
2D-игра на pygame с открытым миром, инвентарём и верстаком с **локальной семантической генерацией крафта** — игрок сам придумывает рецепты, раскладывая предметы на 3×3 сетке, а движок определяет результат по смыслу.

---

## Архитектура (минимум файлов)

```
minacraft/
├── main.py          # точка входа, игровой цикл, менеджер состояний
├── world.py         # генерация 2D-мира, тайлы, ресурсы, физика игрока
├── inventory.py     # инвентарь, слоты, перетаскивание предметов
├── crafting.py      # верстак: UI + семантическая логика крафта
├── items.py         # реестр предметов: данные, теги, свойства
├── assets.py        # загрузка текстур из resources/, fallback-генерация
├── mobs.py          # мобы, боссы, снаряды, дроп предметов, спавн по измерениям
├── CLAUDE.md        # этот файл
└── resources/       # текстуры Faithful 32x Minecraft
```

---

## Источники текстур (resources/Faithful-32x-1.21.11/assets/minecraft/textures/)

### Блоки мира (textures/block/)
- `grass_block_top.png`, `grass_block_side.png` — трава
- `dirt.png` — земля
- `stone.png`, `cobblestone.png` — камень
- `oak_log.png`, `oak_planks.png` — дерево
- `sand.png`, `gravel.png` — песок/гравий
- `coal_ore.png`, `iron_ore.png`, `gold_ore.png`, `diamond_ore.png` — руды

### Предметы (textures/item/)
- Материалы: `stick.png`, `string.png`, `feather.png`, `bone.png`, `flint.png`, `leather.png`
- Руды/слитки: `coal.png`, `iron_ingot.png`, `gold_ingot.png`, `diamond.png`, `copper_ingot.png`
- Инструменты: `wooden_pickaxe.png`, `stone_pickaxe.png`, `iron_pickaxe.png`, `diamond_pickaxe.png`
  и аналогично `_axe`, `_sword`, `_shovel`, `_hoe`
- Броня: `leather_helmet.png`, `iron_chestplate.png`, `diamond_boots.png`, и т.д.
- Еда: `apple.png`, `bread.png`, `beef.png`, `baked_potato.png`
- Прочее: `bow.png`, `arrow.png`, `book.png`, `bowl.png`, `bucket.png`

---

## Система предметов (items.py)

Каждый предмет — это dataclass с полями:
```python
@dataclass
class Item:
    id: str           # уникальный ключ
    name: str         # отображаемое имя
    texture: str      # путь к .png или ключ для fallback
    tags: set[str]    # семантические теги: {"wood", "sharp", "tool", ...}
    properties: dict  # {"damage": 4, "durability": 250, "food_value": 5, ...}
    max_stack: int    # 1 для инструментов/брони, 64 для материалов
    durability: int   # текущая прочность (0 = нет ограничений)
    max_durability: int
```

### Начальные предметы мира:
- `oak_log` — бревно дуба (теги: wood, material)
- `stone` — камень (теги: stone, material)
- `coal` — уголь (теги: fuel, material)
- `iron_ore` → (плавка не реализована) `iron_ingot` (теги: metal, material)
- `stick` — палка (теги: wood, thin, material)
- `flint` — кремень (теги: stone, sharp, material)

---

## Семантическая логика крафта (crafting.py)

Без внешнего ИИ — локальные правила на тегах.

### Алгоритм определения результата:
1. Собрать все теги всех предметов на сетке 3×3
2. Посчитать количество каждого предмета
3. Применить **правила приоритета** (от более специфичных к общим):

```
Правила (примеры):
- 2+ wood + 1+ stone_sharp → stone_axe  (дерево + кремень = каменный топор)
- 3 wood (в колонку) → 2 stick          (доска → палки)
- 2 stick + 3 stone (Т-форма) → stone_pickaxe
- metal + wood_handle → metal_sword      (металл + рукоять = меч)
- leather × 4 → leather_helmet          (кожа = шлем)
- стол × 4 + любой инструмент → ?       (улучшение)
```

4. Если правило не найдено → ИИ-фолбек: генерация "странного" предмета по тегам

### Генерация названия для неизвестных комбинаций:
```python
def generate_name(tags: set, counts: dict) -> str:
    # Шаблоны: "Зачарованный [материал]", "[прилагат.] [существит.]"
    # Простой lookup-словарь тег→слово
```

---

## Игровой мир (world.py)

- Тайловый 2D-мир (32×32 px тайл), скроллинг камеры
- Слои: небо, поверхность, подземелье
- Простая генерация: перлин-шум (или простой рандом) для рельефа
- Ресурсы на поверхности: деревья, камень; под землёй: руды
- Игрок: движение WASD, прыжок, добыча ПКМ/ЛКМ

---

## Инвентарь и UI (inventory.py)

- **Хотбар**: 9 слотов внизу экрана
- **Инвентарь**: 3×9 = 27 слотов (открывается на E)
- **Верстак**: 3×3 входные слоты + 1 выходной (открывается при взаимодействии)
- **Две руки**: левая и правая (отображаются отдельно)
- Перетаскивание drag-and-drop как в Minecraft:
  - ЛКМ: взять/положить весь стак
  - ПКМ: взять/положить половину
  - Shift+ЛКМ: быстрый перенос

---

## Технические решения

| Проблема | Решение |
|---|---|
| Локальный крафт | Система тегов + lookup-таблица правил |
| Генерация картинки | Взять текстуру ближайшего предмета по тегам; если нет — генерировать pygame.Surface с цветом по тегам |
| Прочность | Отдельное поле `durability` в Item, отображается полоской под иконкой |
| Перфоманс | Мир рендерится только в пределах камеры; текстуры кешируются |

---

## Прогресс разработки

### Этапы:
- [x] **Этап 0** — Создать CLAUDE.md, изучить ресурсы ✅
- [x] **Этап 1** — `items.py` + `assets.py`: реестр предметов, загрузка текстур ✅
  - 95 предметов с тегами, прочностью, свойствами
  - Загрузка текстур из Faithful 32x, кеш, fallback-цвет+буква
  - ItemStack: drag-and-drop ready (split_half, can_merge, сериализация)
  - TIERS dict для крафтового движка
- [x] **Этап 2** — `world.py`: генерация 2D-мира, рендер тайлов, движение игрока ✅
  - Тайловый мир 280×130 (8960×4160 пикс), случайный рельеф из 6 синусоид
  - Слои: небо → трава → земля (3 слоя) → камень → руды → бедрок
  - 55 пещер (червеобразный random-walk), 5 видов руд, деревья, песчаные пятна
  - Физика: гравитация, прыжок, AABB-коллизии по осям раздельно
  - Добыча: LMB удерживать → прогресс по твёрдости, бонус от инструмента
  - Размещение: RMB → ставит блок из хотбара
  - Плавная камера со сглаживанием, ограничена краями мира
  - Рендер: только видимые тайлы, градиентное небо
  - Персонаж: нарисован кодом (голова, торс, ноги, глаз, полоска HP)
- [x] **Этап 3** — `inventory.py`: хотбар, инвентарь, drag-and-drop ✅
  - Inventory: 36 слотов (хотбар 9 + грид 3×9=27), сериализация
  - LMB: взять/положить весь стак; RMB: взять/положить половину/один
  - Shift+LMB: мгновенный перенос хотбар↔грид с авто-мерджем
  - Скроллинг колёсиком и клавиши 1-9 для выбора хотбара
  - E: открыть/закрыть инвентарь, при закрытии возвращает курсор в инвентарь
  - Тултип на наведении: название, урон/защита, еда, прочность, уровень добычи
  - Иконка держимого предмета в правом нижнем углу с названием
  - Полоска прочности под предметами (зелёная→жёлтая→красная)
- [x] **Этап 4** — `crafting.py`: UI верстака + семантическая логика ✅
  - Семантический движок: правила по тегам (head_material, handle, armor_material, tier)
  - 12 правил: брёвна→доски, доски→палки, плавка, факелы, огниво, стрела, лук, кирки, мечи, лопаты, топоры/мотыги, броня
  - _mystery_item: генерирует новый предмет на лету, регистрирует его динамически
  - CraftingUI: 3×3 сетка + выход + инвентарь игрока, drag-and-drop, тултипы
- [x] **Этап 5** — `main.py`: связать всё воедино, игровой цикл ✅
  - Менеджер состояний: resolution → world_select → playing ↔ paused
  - ResolutionMenu: 960×600 / 1280×720 / 1920×1080
  - WorldSelectMenu: до 6 слотов, создание/удаление, JSON в saves/
  - PauseMenu: продолжить / сохранить и выйти
  - Автосохранение каждые 60 секунд
  - Верстак открывается ПКМ по блоку workbench в мире (в досягаемости 5 тайлов)
  - Верстак ставится рядом со спавном при создании нового мира
- [x] **Этап 6** — Полировка: звуки, анимации, баланс ✅
  - `sounds.py`: 9 процедурных звуков через numpy (без внешних файлов)
    - dig_grass/stone/wood/sand, break_block, place_block, pickup, jump, craft, cant_mine
  - Анимация ходьбы: ноги качаются через sin(walk_frame), затихают на месте
  - Анимация добычи: рука вытягивается к блоку с swing-отдачей по progress
  - Частицы при разрушении: 5-9 цветных частиц с гравитацией, fade по life
  - Баланс инструментов: TILE_MIN_TIER — руды требуют кирку нужного тира (деревянная ≥ для coal/iron, железная ≥ для gold/diamond), неправильный инструмент = 0 скорость
  - Звуковые события из try_mine: кортеж (drop, sound_event), звук каждые 25% прогресса

---

## Задачи v0.9-alfa (текущая версия)

### Ч1: Версия игры + навигация
- [x] `GAME_VERSION = "0.9-alfa"` в world.py ✅
- [x] Показывать версию на главном экране (ResolutionMenu + WorldSelectMenu) ✅
- [x] Изменить ESC: игра → меню миров (через паузу с кнопкой «Главное меню») ✅
- [x] Добавить «Выйти из игры» на экран выбора мира ✅
- [x] Версия сохраняется в JSON мира ✅

### Ч2: Расширение мира (×10) + sparse save
- [x] `WORLD_W = 2800` ✅
- [x] Архитектура `_base` (сгенерированный мир) + `_mods` (изменения игрока) ✅
- [x] `to_dict` сохраняет только `_mods`, восстанавливает по сиду ✅
- [x] Обратная совместимость со старыми сохранениями (поле `tiles`) ✅

### Ч3: Новые блоки
- [x] `granite`, `andesite`, `diorite` — каменные варианты ✅
- [x] `mossy_cobblestone` — для структур ✅
- [x] `clay` — рядом с водой ✅
- [x] `sandstone` — под песком ✅
- [x] `snow`, `ice` — зимние зоны ✅
- [x] `obsidian` — глубоко под землёй ✅
- [x] `glass` — прозрачный блок (для домов) ✅
- [x] `sponge` — блок-губка ✅
- [x] `water` — жидкость ✅
- [x] `furnace` — печь ✅
- [x] `chest` — сундук ✅
- [x] `barrel` — бочка ✅
- [x] `oak_door` / `oak_door_open` — дверь (ПКМ = переключить) ✅
- [x] `cactus` — кактус (в пустынях) ✅

### Ч4: Вода + озёра + губка
- [x] Генерация озёр на поверхности ✅
- [x] Стекание воды вниз (tick-система, только видимая область) ✅
- [x] Губка (sponge): при размещении удаляет воду в радиусе 4 ✅

### Ч5: Структуры
- [x] Деревянные домики (oak_planks стены, glass окна, door вход) ✅
- [x] Подземные руины (mossy_cobblestone, иногда сундук) ✅

### Ч6: Контейнеры (сундуки, бочки)
- [x] `ContainerUI` — 27 слотов, drag-and-drop ✅
- [x] ПКМ по сундуку/бочке → открыть UI ✅
- [x] Содержимое сохраняется в `world._containers` (часть `_mods`) ✅
- [x] При разрушении — предметы возвращаются в инвентарь ✅

### Ч7: Верстак — режимы крафта
- [x] Кнопка-переключатель «Семантика / Ванильный» в верстаке ✅
- [x] Ванильные крафты: верстак, печь, сундук, бочка, дверь ✅
- [x] Ванильный режим: точное позиционное совпадение по тегам ✅

### Ч8: Сплавы металлов
- [x] `steel_ingot` = 2× iron_ingot + 1× coal (в сетке) ✅
- [x] `bronze_ingot` = 2× copper_ingot + 1× iron_ingot ✅
- [x] Инструменты и броня из стали и бронзы ✅

---

## Задачи v0.9.1-alfa (баг-фикс + новые фичи)

### Ч1: Критические баги ✅
- [x] Клавиатура: WASD/E/1-9 не работают на русской раскладке → keys.py, нормализация по scancode ✅
- [x] Дрожание игрока → обнулять vy вниз на земле перед гравитацией ✅
- [x] Текстура сундука → процедурная отрисовка сундука в assets.py (крышка + засов) ✅
- [x] Кактус не ломается снизу вверх → cascade-break при ломании кактуса ✅
- [x] Крафт: доски + сырое железо дают железные слитки → _r_smelt принимает только coal/charcoal ✅

### Ч2: Физика воды ✅
- [x] Нельзя сломать воду руками ✅
- [x] Замедление движения в воде (SWIM_SPEED = 2.5 vs 3.5) ✅
- [x] Плавание (W/Space толкают вверх, водное сопротивление) ✅
- [x] Задыхание: полоска кислорода, урон 2 HP/сек при 0 кислорода ✅

### Ч3: Дверь в 2 блока ✅
- [x] Дверь занимает 2 тайла (oak_door + oak_door_top) ✅
- [x] Текстуры block/oak_door_bottom и block/oak_door_top из Faithful ✅
- [x] При ломании любой части — удаляются обе (auto-cascade в world.set) ✅
- [x] Открытая дверь рендерится с 80% прозрачностью ✅

### Ч4: Генерация мира (value noise + fBm) ✅
- [x] _ValueNoise1D + _fbm1d: плавный шум с многооктавным fBm ✅
- [x] _ValueNoise2D: 2D шум для пещер (threshold + червеобразные тоннели) ✅
- [x] Biome-система: tundra/mountains/forest/plains/desert (однооктавный шум) ✅
- [x] Биом влияет на поверхность (sand/snow/grass) и высоту рельефа ✅
- [x] Озёра не спавнятся в пустыне ✅

### Ч5: Деревни и структуры ✅
- [x] Меньше одиночных домов: 4-8 вместо ×18 (абсолютно, не масштабируемо) ✅
- [x] _try_village: 3-6 домов + мощёная дорожка + колодец ✅
- [x] Деревни размещаются в более плоских биомах (проверка перепада высот) ✅

### Ч6: Два слоя (foreground/background) ✅
- [x] _bg_base (заполнен камнем/землёй) + _bg_mods (изменения игрока) ✅
- [x] Отрисовка фона (50% яркости) там, где нет переднего плана ✅
- [x] Tab переключает активный слой (get_bg/set_bg, try_mine/try_place с layer=) ✅
- [x] HUD: индикатор активного слоя (желтый = задний) ✅

### Ч7: Система брони ✅
- [x] 4 слота брони в InventoryUI (шлем, нагрудник, штаны, сапоги) ✅
- [x] Shift+ЛКМ на броне → надеть/снять; проверка типа при drag-and-drop ✅
- [x] Урон от утопания уменьшается на defense (параметр update_player) ✅
- [x] Щиты защиты отображаются под полоской HP (синие, до 10 иконок) ✅

---

## Задачи v1.0-beta (текущая версия)

### Ч1: Мобы (mobs.py) ✅
- [x] Базовая система мобов: Mob, Projectile, DroppedItem с физикой и гравитацией ✅
- [x] Пассивные мобы: pig, cow (спавн в оверворлде) ✅
- [x] Враждебные мобы: zombie, skeleton, spider, creeper, villager ✅
- [x] Мобы Незера: zombified_piglin, blaze, ghast + процедурный спавн ✅
- [x] Эндермены во всех мирах (оверворлд, незер, Край) ✅
- [x] Система боссов: фазы, спецатаки, summon_queue для миньонов ✅
- [x] Скелет-Король: 3 стрелы, призыв скелетов в фазе 2 ✅
- [x] Лесной Голем: регенерация + залп из 6 валунов в фазе 2 ✅
- [x] Дракон Края: летает, спираль из 8 огненных шаров в фазе 2 ✅
- [x] Дроп предметов физически (DroppedItem: гравитация, боб, despawn) ✅
- [x] Плавающий текст урона / крита над мобами ✅
- [x] Критический удар: ×1.5 урона в прыжке при vy > 2 ✅
- [x] Система XP и уровней игрока ✅

### Ч2: Измерение Края + финал игры ✅
- [x] Генерация Края: арена из end_stone, обелиски, парящие острова, chorus_plant ✅
- [x] 4 рамки портала end_portal_frame — активируются глазом Края ✅
- [x] Портал в Край: стоять 3 сек в end_portal → телепортация ✅
- [x] spawn_end_mobs: Дракон Края + 10 эндерменов ✅
- [x] Победа: убить Дракона → _game_won, финальный экран ✅
- [x] Финальный экран: философский диалог двух голосов (A/B), прокрутка как End Poem ✅

### Ч3: Полировка и баг-фиксы ✅
- [x] Кнопка полного экрана: F11 везде + кнопки в меню разрешения, миров и паузы ✅
- [x] pygame.FULLSCREEN без SCALED — SCALED крашит из-за отсутствия SDL2-рендерера ✅
- [x] Панель кузницы: инвентарь (9 слотов) вылезал за рамку → panel_rect по всем слотам ✅
- [x] GAME_VERSION = "1.0-beta" ✅

### Ч4: Логирование действий игрока ✅
- [x] `logging.basicConfig` в main.py — вывод в stdout с временными метками `HH:MM:SS` ✅
- [x] Старт игры, загрузка/создание мира ✅
- [x] Добыча блоков (блок, координаты, дроп) ✅
- [x] Размещение блоков ✅
- [x] Крафт предметов (верстак и стол кузница) — в crafting.py ✅
- [x] Открытие UI: инвентарь, верстак, кузница, печь, сундуки, зачарования, варка ✅
- [x] Атака моба (с пометкой [КРИТ]) и смерть моба ✅
- [x] Урон по игроку с источником (моб, снаряд, лава, магма, взрыв) ✅
- [x] Подбор предметов ✅
- [x] Еда ✅
- [x] Повышение уровня ✅
- [x] Смена измерения ✅
- [x] Вызов босса, смерть игрока, победа над Драконом ✅

---

## Задачи v1.0-beta-patch1 (выполнено)

### Реализованные изменения:
- [x] Исправлен баг перекрытия инвентаря в FurnaceUI: `hb_y = inv_top + GRID_ROWS * SS + 6` ✅
- [x] Обсидиан теперь выпадает при добыче алмазной киркой (TILE_DATA drop: None → "obsidian") ✅
- [x] Пули: max_stack = 9999 (была 64) ✅
- [x] Рецепт: 1 железный слиток → 9 железных самородков ✅
- [x] Пули толще: radius 3→5, line_width 2→4 в mobs.py ✅
- [x] Огнемёт лечит блейзов вместо урона ✅
- [x] Пули разрушают блоки (миниган — все кроме бедрока; дробь — только мягкие) ✅
- [x] Ограничение угла стрельбы: нельзя целиться выше 60° от горизонтали ✅
- [x] Мерцание дульного пламени при стрельбе из минигана (оранжевые частицы) ✅
- [x] Улучшена отрисовка брони на игроке (pauldrons, knee plate, visor slit, rivets) ✅
- [x] Добавлен внутриигровой чат (T — открыть, Enter — отправить) ✅
  - Команды: `/give <item> [count]`, `/tp <x> <y>`, `/heal`, `/time day|night`

### ⚠️ Известная проблема при компиляции (PyInstaller / .app):
- **Скины не работают в .app**: `SKINS_DIR` в `main.py` определён как относительный путь
  `Path("resources/Faithful-32x-1.21.11/.../wide")` — не резолвится в frozen-приложении.
- **Исправление**: заменить на `_pkg_dir() / "resources/Faithful-32x-1.21.11/assets/minecraft/textures/entity/player/wide"`,
  где `_pkg_dir()` из `assets.py` возвращает `sys._MEIPASS` для frozen и `Path(".")` иначе.
- Пока не исправлено — применить перед следующей сборкой.

---

## Лаунчер (launcher.py) — добавлен после v0.9.1-alfa

### Файлы:
```
launcher.py              # точка входа (tkinter, тёмная тема)
launcher/
  __init__.py
  config.py              # launcher_config.json: repo, активная версия
  github_api.py          # GitHub Releases API (urllib, без зависимостей)
  version_mgr.py         # установка/удаление версий, реестр миров
versions/                # установленные версии (каждая — папка с main.py)
```

### Функции:
- Список версий с GitHub Releases (+ "Dev" — текущая папка)
- Скачивание и установка версий с прогресс-баром (в фоновом потоке)
- Откат на любую версию
- Управление мирами (список, создание, удаление)
- Запуск выбранной версии с выбранным миром
- Лаунчер скрывается пока игра запущена, возвращается после закрытия
- Настройки: GitHub repo (owner/repo)

### Переменные окружения (launcher → main.py):
- `MINACRAFT_WORLD_SLOT` — слот мира (int), пропускает экран выбора
- `MINACRAFT_SAVES_DIR`  — путь к папке сохранений (общей для всех версий)

### GitHub: чтобы версии можно было скачивать:
1. Создать GitHub-репо с кодом игры
2. Публиковать GitHub Releases с тегом (`0.9-alfa`, `0.9.1-alfa`, …)
3. Прикладывать .zip с кодом (или использовать автогенерируемый source.zip)

---

## Решения по дизайну (подтверждены)

| Вопрос | Ответ |
|---|---|
| Вид | **Платформер сбоку** (как Terraria) — гравитация, прыжки, копаем вниз и вбок |
| Мобы | Реализованы в mobs.py: пассивные, враждебные, боссы, Незер, Край |
| Сохранение | **Несколько слотов миров** — как в Minecraft/Terraria, JSON-файлы в папке `saves/` |
| Разрешение | **Выбор при запуске**: 960×600 / 1280×720 / 1920×1080 |
| Размер тайла | 32×32 px (совпадает с Faithful 32x) |

## Физика платформера
- Гравитация: постоянное ускорение вниз
- Прыжок: Space / W
- Движение: A/D или ←/→
- Добыча: ЛКМ по тайлу рядом с игроком
- Взаимодействие (верстак): ПКМ / E на блок верстака
